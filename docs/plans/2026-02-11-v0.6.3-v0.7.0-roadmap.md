# DotCor v0.6.3 → v0.7.0 Roadmap Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

## Overview

This plan covers the polish roadmap from v0.6.2 (all features implemented) to v1.0.0 (production release):
- **v0.6.3**: Critical code fixes (syntax errors, cleanup)
- **v0.6.4**: UX Polish Part 1 (git progress, better prompts)
- **v0.6.5**: UX Polish Part 2 (colors, tables)
- **v0.6.6**: Reliability Part 1 (pre-flight validation)
- **v0.6.7**: Reliability Part 2 (lock handling)
- **v0.6.8**: Simple Features Part 1
- **v0.6.9**: Simple Features Part 2
- **v0.7.0**: Final Polish & Documentation

**Total Estimated Effort:** ~35-50 hours (~5-7 days)

---

## Tech Stack

- Go 1.21+
- Cobra CLI framework
- Existing test patterns in `cmd/dotcor/*_test.go`
- testify/assert and testify/require for assertions

## Pre-Commit Checklist

Before every commit, run:
```bash
go build ./... && go test ./... && golangci-lint run ./...
```

If lint errors occur, fix them before committing.

---

# Part 1: v0.6.3 - Critical Code Fixes

**Theme:** Fix bugs and cleanup code quality
**Effort:** ~2-3 hours

## Architecture

Fixes three categories of issues:
1. Syntax errors that prevent compilation
2. Remove non-standard comment separators
3. Fix typos in comments and error messages

---

## Task 1.1: Fix Syntax Error in sync.go:244

**Files:**
- Modify: `cmd/dotcor/sync.go:244`

**Step 1: Read sync.go and locate syntax error**

```bash
cd /Users/justincordova/cs/dotcor
head -n 250 cmd/dotcor/sync.go | tail -n 10
```

Expected output: Shows the malformed `} else if` statement.

**Step 2: Fix the syntax error**

```go
// Before (around line 244):
} else if gitStatus.BehindBy > 0 {

// After:
} else {
    if gitStatus.BehindBy > 0 {
```

**Step 3: Verify fix with build**

```bash
go build ./cmd/dotcor/...
```

Expected: No compilation errors.

**Step 4: Commit**

```bash
git add cmd/dotcor/sync.go
git commit -m "fix: correct syntax error in sync.go condition"
```

---

## Task 1.2: Remove "=====" Separator Comments

**Files:**
- Modify: `internal/core/transaction.go:112,114,356,358`
- Modify: `cmd/dotcor/doctor.go:41`

**Step 1: Read transaction.go to find separator comments**

```bash
grep -n "^// ===" internal/core/transaction.go
```

Expected: Shows lines 112, 114, 356, 358.

**Step 2: Remove all separator comments from transaction.go**

For each occurrence (lines 112, 114, 356, 358), replace with blank line.

**Step 3: Read doctor.go to find separator line**

```bash
grep -n "=======" cmd/dotcor/doctor.go
```

Expected: Shows line 41 with extra `=` at end.

**Step 4: Fix separator line in doctor.go**

```go
// Before (around line 41):
fmt.Println("=============")

// After:
fmt.Println("============")
```

**Step 5: Build to verify**

```bash
go build ./...
```

**Step 6: Commit**

```bash
git add internal/core/transaction.go cmd/dotcor/doctor.go
git commit -m "style: remove non-standard separator comments"
```

---

## Task 1.3: Fix Typos in Core Files (Batch 1)

**Files:**
- Modify: `internal/core/validator.go`
- Modify: `internal/core/backup.go`
- Modify: `internal/core/lock.go`

**Step 1: Fix validator.go typos**

```bash
grep -n "validating\|Can't\|isn't" internal/core/validator.go
```

Fix all occurrences:
- "validating" → "validating"
- "Can't" → "Cannot"
- "isn't" → "is not"

**Step 2: Fix backup.go typos**

```bash
grep -n "calculating\|Can't\|can't" internal/core/backup.go
```

Fix all occurrences:
- "calculating" → "calculating"
- "Can't" → "Cannot"
- "can't" → "cannot"

**Step 3: Fix lock.go typos**

```bash
grep -n "Can't" internal/core/lock.go
```

Fix all occurrences:
- "Can't" → "Cannot"

**Step 4: Build and test**

```bash
go build ./... && go test ./internal/core/...
```

**Step 5: Commit**

```bash
git add internal/core/validator.go internal/core/backup.go internal/core/lock.go
git commit -m "fix: correct typos in core files"
```

---

## Task 1.4: Fix Typos in Config and Ignore Files

**Files:**
- Modify: `internal/config/paths.go`
- Modify: `internal/core/ignore.go`

**Step 1: Fix paths.go typos**

```bash
grep -n "generating\|can't" internal/config/paths.go
```

Fix all occurrences:
- "generating" → "generating"
- "can't" → "cannot"

**Step 2: Fix ignore.go typo**

```bash
grep -n "normalizedName" internal/core/ignore.go
```

Fix:
```go
// Before (line 32):
normalizedPattern := filepath.FromSlash(pattern)

// After:
normalizedPattern := filepath.FromSlash(pattern)
```

**Step 3: Build and test**

```bash
go build ./... && go test ./internal/config/... ./internal/core/...
```

**Step 4: Commit**

```bash
git add internal/config/paths.go internal/core/ignore.go
git commit -m "fix: correct typos in config and ignore files"
```

---

## Task 1.5: Fix Typos in Command Files (Batch 1)

**Files:**
- Modify: `cmd/dotcor/list.go`
- Modify: `cmd/dotcor/doctor.go`
- Modify: `cmd/dotcor/remove.go`

**Step 1: Fix list.go typo**

```bash
grep -n "not-symlink" cmd/dotcor/list.go
```

Fix:
```go
// Before (line 218):
return "not-symlink"

// After:
return "not_symlink"
```

**Step 2: Fix doctor.go function name typo**

```bash
grep -n "func check" cmd/dotcor/doctor.go | head -20
```

Review function names for consistency. The file appears correct but verify function names match their usage.

**Step 3: Fix remove.go typo**

```bash
grep -n "specify" cmd/dotcor/remove.go
```

Fix:
```go
// Before (line 48):
return fmt.Errorf("specify files to remove or use --all")

// After:
return fmt.Errorf("specify files to remove or use --all")
```

**Step 4: Build and test**

```bash
go build ./... && go test ./cmd/dotcor/...
```

**Step 5: Commit**

```bash
git add cmd/dotcor/list.go cmd/dotcor/doctor.go cmd/dotcor/remove.go
git commit -m "fix: correct typos in command files"
```

---

## Task 1.6: Fix Typo in history.go

**Files:**
- Modify: `cmd/dotcor/history.go`

**Step 1: Fix flag description typo**

```bash
grep -n "oneline" cmd/dotcor/history.go
```

Review the comment on line 31. The issue is inconsistent terminology but the code itself is fine.

**Step 2: Verify no other typos**

```bash
grep -n "Can't\|can't\|specify" cmd/dotcor/history.go
```

**Step 3: Commit if changes needed**

If no typos found, skip this commit. Otherwise:
```bash
git add cmd/dotcor/history.go
git commit -m "fix: correct typo in history.go"
```

---

## Task 1.7: v0.6.3 Final Verification

**Step 1: Build, test, and lint**

```bash
go build ./... && go test ./... && golangci-lint run ./...
```

**Step 2: Update version**

Modify `cmd/dotcor/main.go:15`:
```go
version = "0.6.3" // Critical code fixes
```

**Step 3: Commit and tag**

```bash
git add cmd/dotcor/main.go
git commit -m "chore: bump version to v0.6.3"
git tag -a v0.6.3 -m "Release v0.6.3: Critical code fixes"
```

---

# Part 2: v0.6.4 - UX Polish Part 1: Progress & Prompts

**Theme:** Visible UX improvements
**Effort:** ~6-9 hours

## Architecture

Improve user experience with:
1. Git progress display for slow operations
2. Better confirmation prompts with file summaries
3. Consistent error messages

---

## Task 2.1: Git Progress Display for clone and sync

**Files:**
- Modify: `cmd/dotcor/clone.go:35-39`
- Modify: `cmd/dotcor/sync.go:87-107`

**Step 1: Add progress display to clone command**

In `clone.go`, modify the git clone call:

```go
// Before (around line 35-39):
if err := git.Clone(repoURL, filesDir); err != nil {
    return fmt.Errorf("cloning repository: %w", err)
}

// After:
fmt.Printf("Cloning repository from %s...\n", repoURL)
if err := git.CloneWithProgress(repoURL, filesDir); err != nil {
    return fmt.Errorf("cloning repository: %w", err)
}
fmt.Println("[OK] Repository cloned")
```

**Step 2: Add git.CloneWithProgress function**

Modify `internal/git/git.go`:

```go
// CloneWithProgress clones repository and shows progress
func CloneWithProgress(url, dir string) error {
    if !IsGitInstalled() {
        return fmt.Errorf("git is not installed")
    }

    // Clone with progress
    cmd := exec.Command("git", "clone", "--progress", url, dir)
    cmd.Stdout = os.Stdout
    cmd.Stderr = os.Stderr
    return cmd.Run()
}
```

**Step 3: Add progress display to sync command**

In `sync.go`, modify the git push/pull calls to show progress:

```go
// Before git operations:
fmt.Printf("Syncing changes...\n")

// Use git commands that output progress
cmd.Stdout = os.Stdout
cmd.Stderr = os.Stderr
```

**Step 4: Write test for git progress**

Add to `cmd/dotcor/clone_test.go` and `cmd/dotcor/sync_test.go`:

```go
func TestClone_ProgressShown(t *testing.T) {
    // Arrange - set up temp environment
    tempDir := t.TempDir()
    filesDir := filepath.Join(tempDir, "files")

    // Act - capture stdout
    var buf bytes.Buffer
    originalStdout := os.Stdout
    r, w, _ := os.Pipe()
    os.Stdout = w

    // Run clone command
    cloneCmd.SetArgs([]string{"https://github.com/test/repo.git", filesDir})
    err := runClone(cloneCmd, []string{})

    // Restore stdout
    w.Close()
    os.Stdout = originalStdout
    io.Copy(&buf, r)

    // Assert - Output contains progress messages
    require.NoError(t, err)
    assert.Contains(t, buf.String(), "Cloning repository", "should show cloning message")
}
```

**Step 5: Build, test, and lint**

```bash
go build ./... && go test -v ./cmd/dotcor/... -run "Test.*Progress" && golangci-lint run ./...
```

**Step 6: Commit**

```bash
git add cmd/dotcor/clone.go cmd/dotcor/sync.go internal/git/git.go cmd/dotcor/*_test.go
git commit -m "feat: add git progress display for clone and sync"
```

---

## Task 2.2: Better Confirmation Prompts (Part 1 - Add & Remove)

**Files:**
- Modify: `cmd/dotcor/add.go:94-127`
- Modify: `cmd/dotcor/remove.go:90-106`

**Step 1: Add file summary to add command**

In `add.go`, modify the confirmation section:

```go
// Before (around line 94-127):
if !force {
    fmt.Printf("Add %d file(s)? [y/N]: ", len(filesToAdd))
    reader := bufio.NewReader(os.Stdin)
    input, _ := reader.ReadString('\n')
    // ...
}

// After - Add file summary:
if !force {
    // Calculate total size
    var totalSize int64
    for _, file := range filesToAdd {
        if info, err := os.Stat(file); err == nil {
            totalSize += info.Size()
        }
    }

    fmt.Println("Summary:")
    fmt.Printf("  Files to add: %d\n", len(filesToAdd))
    fmt.Printf("  Total size: %s\n", formatSize(totalSize))
    fmt.Printf("  Ignored files: %d (matched ignore patterns)\n", ignoredCount)
    fmt.Println("")
    fmt.Printf("Proceed? [y/N]: ")
    // ...
}
```

**Step 2: Add formatSize helper function**

Create or modify existing helper in `cmd/dotcor/main.go`:

```go
func formatSize(bytes int64) string {
    const (
        KB = 1024
        MB = KB * 1024
        GB = MB * 1024
    )

    switch {
    case bytes >= GB:
        return fmt.Sprintf("%.1f GB", float64(bytes)/float64(GB))
    case bytes >= MB:
        return fmt.Sprintf("%.1f MB", float64(bytes)/float64(MB))
    case bytes >= KB:
        return fmt.Sprintf("%.1f KB", float64(bytes)/float64(KB))
    default:
        return fmt.Sprintf("%d bytes", bytes)
    }
}
```

**Step 3: Add file summary to remove command**

In `remove.go`, modify the confirmation:

```go
// Before:
if !force {
    fmt.Printf("Remove %d file(s) from management?\n", len(filesToRemove))
    // ...
}

// After - Add backup location:
if !force {
    fmt.Println("Summary:")
    fmt.Printf("  Files to remove: %d\n", len(filesToRemove))
    fmt.Println("  Backups will be preserved")
    fmt.Println("")
    fmt.Printf("Proceed? [y/N]: ")
    // ...
}
```

**Step 4: Write tests for improved prompts**

Add tests to verify summary display.

**Step 5: Build, test, and lint**

```bash
go build ./... && go test ./cmd/dotcor/... -run "Test.*Prompt" && golangci-lint run ./...
```

**Step 6: Commit**

```bash
git add cmd/dotcor/add.go cmd/dotcor/remove.go cmd/dotcor/main.go cmd/dotcor/*_test.go
git commit -m "feat: add file summaries to confirmation prompts"
```

---

## Task 2.3: v0.6.4 Final Verification

**Step 1: Build, test, and lint**

```bash
go build ./... && go test ./... && golangci-lint run ./...
```

**Step 2: Update version**

Modify `cmd/dotcor/main.go:15`:
```go
version = "0.6.4" // UX progress and prompts
```

**Step 3: Commit and tag**

```bash
git add cmd/dotcor/main.go
git commit -m "chore: bump version to v0.6.4"
git tag -a v0.6.4 -m "Release v0.6.4: UX progress and prompts"
```

---

# Part 3: v0.6.5 - UX Polish Part 2: Colors & Tables

**Theme:** Visual polish with colors and formatting
**Effort:** ~4-6 hours

## Architecture

Use ANSI color codes for consistent visual feedback:
- Green (✓): Success
- Yellow (⚠): Warnings
- Red (✗): Errors
- Blue (ℹ): Information
- Gray: Dry-run output

---

## Task 3.1: Add Color Helper Functions

**Files:**
- Modify: `cmd/dotcor/main.go` (add color helpers)

**Step 1: Add ANSI color constants and functions**

```go
// ANSI color codes
const (
    ColorReset  = "\033[0m"
    ColorRed    = "\033[31m"
    ColorGreen  = "\033[32m"
    ColorYellow = "\033[33m"
    ColorBlue   = "\033[34m"
    ColorGray   = "\033[90m"
)

// Success prints success message in green
func Success(msg string) {
    fmt.Printf("%s✓%s %s\n", ColorGreen, ColorReset, msg)
}

// Warning prints warning message in yellow
func Warning(msg string) {
    fmt.Printf("%s⚠%s %s\n", ColorYellow, ColorReset, msg)
}

// Error prints error message in red
func Error(msg string) {
    fmt.Printf("%s✗%s %s\n", ColorRed, ColorReset, msg)
}

// Info prints info message in blue
func Info(msg string) {
    fmt.Printf("%sℹ%s %s\n", ColorBlue, ColorReset, msg)
}

// DryRun prints dry-run message in gray
func DryRun(msg string) {
    fmt.Printf("%s•%s %s\n", ColorGray, ColorReset, msg)
}
```

**Step 2: Build to verify**

```bash
go build ./...
```

**Step 3: Commit**

```bash
git add cmd/dotcor/main.go
git commit -m "feat: add color helper functions"
```

---

## Task 3.2: Apply Colors to Command Output

**Files:**
- Modify: All command files (`cmd/dotcor/*.go`)

**Step 1: Update success messages in add.go**

Find and replace:
```go
// Before:
fmt.Printf("  [OK] Added %s\n", mf.SourcePath)

// After:
Success(fmt.Sprintf("Added %s", mf.SourcePath))
```

**Step 2: Update error messages**

Find and replace:
```go
// Before:
fmt.Printf("  [X] %s\n", error)

// After:
Error(error)
```

**Step 3: Update warning messages**

Find and replace:
```go
// Before:
fmt.Printf("  [!] %s\n", warning)

// After:
Warning(warning)
```

**Step 4: Apply to all commands**

Repeat for all commands:
- remove.go
- sync.go
- status.go
- restore.go
- clone.go
- doctor.go
- adopt.go
- rebuild.go
- rebuild-links.go
- cleanup.go

**Step 5: Build and test**

```bash
go build ./... && go test ./cmd/dotcor/...
```

**Step 6: Commit**

```bash
git add cmd/dotcor/*.go
git commit -m "feat: apply color coding to all command output"
```

---

## Task 3.3: Improve Table Formatting

**Files:**
- Modify: `cmd/dotcor/list.go`
- Modify: `cmd/dotcor/status.go`

**Step 1: Calculate dynamic column widths**

In `list.go`, modify `outputLong` function:

```go
// Before (around line 100-124):
func outputLong(cfg *config.Config, files []config.ManagedFile, showStatus bool) error {
    w := tabwriter.NewWriter(os.Stdout, 0, 0, 2, ' ', 0)
    // ...
}

// After:
func outputLong(cfg *config.Config, files []config.ManagedFile, showStatus bool) error {
    // Calculate column widths
    maxSource := 20
    maxRepo := 20
    maxAdded := 12
    maxStatus := 10

    for _, f := range files {
        if len(f.SourcePath) > maxSource {
            maxSource = len(f.SourcePath)
        }
        if len(f.RepoPath) > maxRepo {
            maxRepo = len(f.RepoPath)
        }
    }

    w := tabwriter.NewWriter(os.Stdout, 0, 0, 2, ' ', 0)
    fmt.Fprintf(w, "%-*s\t%-*s\t%-*s", maxSource, "SOURCE", maxRepo, "REPO PATH", maxAdded, "ADDED")
    if showStatus {
        fmt.Fprintf(w, "\t%-*s", maxStatus, "STATUS")
    }
    fmt.Fprintln(w)

    for _, f := range files {
        addedAt := f.AddedAt.Format("2006-01-02")

        if showStatus {
            status := getSymlinkStatus(cfg, f)
            fmt.Fprintf(w, "%-*s\t%-*s\t%-*s\t%-*s\n", maxSource, f.SourcePath, maxRepo, f.RepoPath, maxAdded, addedAt, maxStatus, status)
        } else {
            fmt.Fprintf(w, "%-*s\t%-*s\t%-*s\n", maxSource, f.SourcePath, maxRepo, f.RepoPath, maxAdded, addedAt)
        }
    }

    w.Flush()
    fmt.Printf("\n%d file(s) managed\n", len(files))
    return nil
}
```

**Step 2: Add colored status indicators**

Modify `getSymlinkStatus` in `list.go` to return color-coded status:

```go
func getSymlinkStatus(cfg *config.Config, f config.ManagedFile) string {
    // ... existing status checking ...

    // Return color-coded status
    if status == "ok" {
        return fmt.Sprintf("%s✓%s ok", ColorGreen, ColorReset)
    } else if status == "broken" {
        return fmt.Sprintf("%s✗%s broken", ColorRed, ColorReset)
    } else if status == "missing" {
        return fmt.Sprintf("%s✗%s missing", ColorRed, ColorReset)
    } else {
        return fmt.Sprintf("%s⚠%s %s", ColorYellow, ColorReset, status)
    }
}
```

**Step 3: Add smart path truncation**

```go
func truncatePath(path string, maxLen int) string {
    if len(path) <= maxLen {
        return path
    }

    // Keep first and last parts
    keepStart := maxLen/2 - 2
    keepEnd := maxLen/2 - 3

    return path[:keepStart] + "..." + path[len(path)-keepEnd:]
}
```

**Step 4: Apply same improvements to status.go**

Repeat for `status.go` tables.

**Step 5: Build and test**

```bash
go build ./... && go test ./cmd/dotcor/... -run "TestList\|TestStatus"
```

**Step 6: Commit**

```bash
git add cmd/dotcor/list.go cmd/dotcor/status.go
git commit -m "feat: improve table formatting with dynamic widths and colors"
```

---

## Task 3.4: Fix Nil Slice Initialization

**Files:**
- Modify: `cmd/dotcor/sync.go:48`

**Step 1: Fix nil slice**

```go
// Before (line 48):
var filesToSyncRepoPaths []string

// After:
filesToSyncRepoPaths := []string{}
```

**Step 2: Build and test**

```bash
go build ./... && go test ./cmd/dotcor/sync_test.go
```

**Step 3: Commit**

```bash
git add cmd/dotcor/sync.go
git commit -m "fix: initialize filesToSyncRepoPaths to empty slice"
```

---

## Task 3.5: v0.6.5 Final Verification

**Step 1: Build, test, and lint**

```bash
go build ./... && go test ./... && golangci-lint run ./...
```

**Step 2: Update version**

Modify `cmd/dotcor/main.go:15`:
```go
version = "0.6.5" // UX colors and tables
```

**Step 3: Commit and tag**

```bash
git add cmd/dotcor/main.go
git commit -m "chore: bump version to v0.6.5"
git tag -a v0.6.5 -m "Release v0.6.5: UX colors and tables"
```

---

# Part 4: v0.6.6 - Reliability Part 1: Pre-flight Validation

**Theme:** Pre-operation safety checks
**Effort:** ~6-8 hours

## Architecture

Add validation phase before operations that:
1. Checks symlinks exist and are valid
2. Verifies repo files exist
3. Confirms backup directory available
4. Checks git status

Display results with status indicators before proceeding.

---

## Task 4.1: Create Pre-flight Validation Functions

**Files:**
- Create: `internal/core/validation.go`

**Step 1: Create validation.go with validation functions**

```go
package core

import (
    "fmt"
    "os"
    "path/filepath"

    "github.com/justincordova/dotcor/internal/config"
    "github.com/justincordova/dotcor/internal/fs"
)

// ValidationResult represents the result of pre-flight checks
type ValidationResult struct {
    Success  bool
    Checks   []CheckResult
}

// CheckResult represents a single validation check
type CheckResult struct {
    Name    string
    Passed  bool
    Message string
}

// RunPreflightValidation runs all pre-flight checks for an operation
func RunPreflightValidation(cfg *config.Config, operation string, files []string) ValidationResult {
    var checks []CheckResult

    // Check 1: Symlinks exist and are valid
    for _, mf := range cfg.ManagedFiles {
        sourcePath, err := config.ExpandPath(mf.SourcePath, cfg)
        if err != nil {
            checks = append(checks, CheckResult{
                Name:    "symlink_valid",
                Passed:  false,
                Message: fmt.Sprintf("%s: cannot expand path: %v", mf.SourcePath, err),
            })
            continue
        }

        isLink, err := fs.IsSymlink(sourcePath)
        if err != nil {
            checks = append(checks, CheckResult{
                Name:    "symlink_valid",
                Passed:  false,
                Message: fmt.Sprintf("%s: cannot check symlink: %v", mf.SourcePath, err),
            })
            continue
        }

        if !isLink {
            checks = append(checks, CheckResult{
                Name:    "symlink_valid",
                Passed:  false,
                Message: fmt.Sprintf("%s: not a symlink", mf.SourcePath),
            })
            continue
        }

        // Check if symlink is valid
        valid, err := fs.IsValidSymlink(sourcePath)
        if err != nil || !valid {
            checks = append(checks, CheckResult{
                Name:    "symlink_valid",
                Passed:  false,
                Message: fmt.Sprintf("%s: broken symlink", mf.SourcePath),
            })
            continue
        }

        checks = append(checks, CheckResult{
            Name:    "symlink_valid",
            Passed:  true,
            Message: fmt.Sprintf("%s: symlink exists and is valid", mf.SourcePath),
        })
    }

    // Check 2: Repo files exist
    for _, mf := range cfg.ManagedFiles {
        repoPath, err := config.GetRepoFilePath(cfg, mf.RepoPath)
        if err != nil {
            checks = append(checks, CheckResult{
                Name:    "repo_file_exists",
                Passed:  false,
                Message: fmt.Sprintf("%s: cannot get repo path: %v", mf.RepoPath, err),
            })
            continue
        }

        if !fs.PathExists(repoPath) {
            checks = append(checks, CheckResult{
                Name:    "repo_file_exists",
                Passed:  false,
                Message: fmt.Sprintf("%s: repo file missing: %s", mf.SourcePath, repoPath),
            })
        } else {
            checks = append(checks, CheckResult{
                Name:    "repo_file_exists",
                Passed:  true,
                Message: fmt.Sprintf("%s: repo file exists", mf.SourcePath),
            })
        }
    }

    // Check 3: Backup directory available
    backupDir, err := core.GetBackupDir()
    if err != nil {
        checks = append(checks, CheckResult{
            Name:    "backup_dir_available",
            Passed:  false,
            Message: fmt.Sprintf("cannot get backup directory: %v", err),
        })
    } else {
        if fs.PathExists(backupDir) {
            checks = append(checks, CheckResult{
                Name:    "backup_dir_available",
                Passed:  true,
                Message: "backup directory exists",
            })
        } else {
            checks = append(checks, CheckResult{
                Name:    "backup_dir_available",
                Passed:  false,
                Message: "backup directory missing",
            })
        }
    }

    // Check 4: Git status (if git enabled)
    if cfg.GitEnabled {
        repoPath, err := config.ExpandPath(cfg.RepoPath, cfg)
        if err == nil {
            hasChanges, _ := git.HasChanges(repoPath)
            if hasChanges {
                checks = append(checks, CheckResult{
                    Name:    "git_clean",
                    Passed:  true,
                    Message: "uncommitted changes in repository",
                })
            } else {
                checks = append(checks, CheckResult{
                    Name:    "git_clean",
                    Passed:  true,
                    Message: "git repository clean",
                })
            }
        }
    }

    // Determine overall success
    success := true
    for _, check := range checks {
        if !check.Passed {
            success = false
        }
    }

    return ValidationResult{
        Success: success,
        Checks:   checks,
    }
}

// DisplayValidationResults shows validation results to user
func DisplayValidationResults(result ValidationResult) error {
    fmt.Println("🔍 Pre-flight checks:")
    fmt.Println("")

    allPassed := true
    for _, check := range result.Checks {
        if check.Passed {
            fmt.Printf("  ✓ %s\n", check.Message)
        } else {
            fmt.Printf("  ✗ %s\n", check.Message)
            allPassed = false
        }
    }

    fmt.Println("")

    if !allPassed {
        return fmt.Errorf("pre-flight checks failed")
    }

    return nil
}
```

**Step 2: Build to verify**

```bash
go build ./internal/core/...
```

**Step 3: Write tests for validation**

Create `internal/core/validation_test.go`:

```go
func TestRunPreflightValidation_AllChecksPass(t *testing.T) {
    t.Run("all checks pass with valid setup", func(t *testing.T) {
        // Arrange
        cfg := CreateTestConfig(t)

        // Create test setup
        // ... create symlinks, repo files, etc.

        // Act
        result := core.RunPreflightValidation(cfg, "test", []string{})

        // Assert
        require.True(t, result.Success, "validation should succeed")
        for _, check := range result.Checks {
            assert.True(t, check.Passed, "all checks should pass")
        }
    })
}
```

**Step 4: Commit**

```bash
git add internal/core/validation.go internal/core/validation_test.go
git commit -m "feat: add pre-flight validation system"
```

---

## Task 4.2: Integrate Pre-flight Checks into Commands

**Files:**
- Modify: `cmd/dotcor/add.go` (add before operation)
- Modify: `cmd/dotcor/remove.go` (add before operation)
- Modify: `cmd/dotcor/restore.go` (add before operation)

**Step 1: Add pre-flight check to add.go**

```go
// Before file operations (around line 94):
// Load config
cfg, err := config.LoadConfig()
// ...

// Add pre-flight validation
result := core.RunPreflightValidation(cfg, "add", args)
core.DisplayValidationResults(result)
if !result.Success {
    return fmt.Errorf("pre-flight checks failed")
}
```

**Step 2: Add pre-flight check to remove.go**

```go
// Before file operations (around line 90):
// Add pre-flight validation
result := core.RunPreflightValidation(cfg, "remove", filesToRemove)
core.DisplayValidationResults(result)
if !result.Success {
    return fmt.Errorf("pre-flight checks failed")
}
```

**Step 3: Add pre-flight check to restore.go**

```go
// Before file operations:
// Add pre-flight validation
result := core.RunPreflightValidation(cfg, "restore", []string{sourcePath})
core.DisplayValidationResults(result)
if !result.Success {
    return fmt.Errorf("pre-flight checks failed")
}
```

**Step 4: Build and test**

```bash
go build ./... && go test ./cmd/dotcor/...
```

**Step 5: Commit**

```bash
git add cmd/dotcor/add.go cmd/dotcor/remove.go cmd/dotcor/restore.go
git commit -m "feat: integrate pre-flight validation into commands"
```

---

## Task 4.3: v0.6.6 Final Verification

**Step 1: Build, test, and lint**

```bash
go build ./... && go test ./... && golangci-lint run ./...
```

**Step 2: Update version**

Modify `cmd/dotcor/main.go:15`:
```go
version = "0.6.6" // Pre-flight validation
```

**Step 3: Commit and tag**

```bash
git add cmd/dotcor/main.go
git commit -m "chore: bump version to v0.6.6"
git tag -a v0.6.6 -m "Release v0.6.6: Pre-flight validation"
```

---

# Part 5: v0.6.7 - Reliability Part 2: Lock Handling

**Theme:** Better lock conflict resolution
**Effort:** ~4-5 hours

## Architecture

Add interactive menu for lock conflicts with options:
1. Wait for lock to release (automatic retry)
2. Force release lock (may cause conflicts)
3. Cancel operation

Show process age and details.

---

## Task 5.1: Improve Lock Error Messages

**Files:**
- Modify: `internal/core/lock.go:70-90`

**Step 1: Add process age to lock errors**

```go
// Before (around line 70-90):
fmt.Printf("Lock held by another process (PID %d on %s)\n", info.PID, info.Hostname)
return fmt.Errorf("%w: PID %d on %s. If this is incorrect, run 'dotcor doctor --fix'", ErrLockHeld, info.PID, info.Hostname)

// After:
age := time.Since(info.Timestamp)
ageStr := formatAge(age)

fmt.Printf("Lock held by another process (PID %d on %s)\n", info.PID, info.Hostname)
fmt.Printf("  Started: %s ago\n", ageStr)
fmt.Println("")
fmt.Println("Options:")
fmt.Println("  1) Wait for lock to release (automatic retry)")
fmt.Println("  2) Force release lock (may cause conflicts)")
fmt.Println("  3) Cancel")
fmt.Print("[1-3]: ")

var choice string
fmt.Scanln(&choice)

switch choice {
case "1":
    // Wait and retry logic
    return waitForLock(cfg)
case "2":
    // Force release
    if err := ForceReleaseLock(cfg); err != nil {
        return fmt.Errorf("forcing lock: %w", err)
    }
    return nil
case "3":
    return fmt.Errorf("operation cancelled")
default:
    return fmt.Errorf("invalid choice")
}

func formatAge(d time.Duration) string {
    if d < time.Minute {
        return fmt.Sprintf("%d seconds", int(d.Seconds()))
    } else if d < time.Hour {
        return fmt.Sprintf("%d minutes", int(d.Minutes()))
    } else {
        return fmt.Sprintf("%.1f hours", d.Hours())
    }
}

func waitForLock(cfg *config.Config) error {
    maxWait := time.Minute * 5
    checkInterval := time.Second * 2

    for elapsed := time.Duration(0); elapsed < maxWait; elapsed += checkInterval {
        time.Sleep(checkInterval)

        lockPath, _ := getLockPath()
        if !fs.PathExists(lockPath) {
            // Lock released
            return AcquireLock(cfg)
        }

        fmt.Printf("\rWaiting for lock... (%s elapsed)", formatAge(elapsed))
    }

    return fmt.Errorf("timed out waiting for lock")
}
```

**Step 2: Build and test**

```bash
go build ./internal/core/...
```

**Step 3: Write tests for lock handling**

Add tests for interactive lock resolution.

**Step 4: Commit**

```bash
git add internal/core/lock.go
git commit -m "feat: add interactive lock conflict resolution"
```

---

## Task 5.2: v0.6.7 Final Verification

**Step 1: Build, test, and lint**

```bash
go build ./... && go test ./... && golangci-lint run ./...
```

**Step 2: Update version**

Modify `cmd/dotcor/main.go:15`:
```go
version = "0.6.7" // Interactive lock handling
```

**Step 3: Commit and tag**

```bash
git add cmd/dotcor/main.go
git commit -m "chore: bump version to v0.6.7"
git tag -a v0.6.7 -m "Release v0.6.7: Interactive lock handling"
```

---

# Part 6: v0.6.8 - Simple Features Part 1

**Theme:** Useful tools for daily workflow
**Effort:** ~4-6 hours

## Architecture

Add small but useful features:
1. Quick status in prompt (--prompt flag)
2. Backup diff command
3. File categories listing (--categories flag)

---

## Task 6.1: Add Quick Status in Prompt

**Files:**
- Modify: `cmd/dotcor/status.go`

**Step 1: Add --prompt flag to status command**

```go
func init() {
    statusCmd.Flags().BoolP("quick", "q", false, "Show quick summary")
    statusCmd.Flags().Bool("problems", false, "Show only files with issues")
    statusCmd.Flags().Bool("json", false, "Output as JSON")
    statusCmd.Flags().Bool("prompt", false, "Output minimal for shell prompts")
    rootCmd.AddCommand(statusCmd)
}
```

**Step 2: Modify runStatus to handle --prompt**

```go
func runStatus(cmd *cobra.Command, args []string) error {
    quick, _ := cmd.Flags().GetBool("quick")
    problemsOnly, _ := cmd.Flags().GetBool("problems")
    jsonFormat, _ := cmd.Flags().GetBool("json")
    prompt, _ := cmd.Flags().GetBool("prompt")

    // Handle --prompt mode
    if prompt {
        cfg, err := config.LoadConfig()
        if err != nil {
            return err
        }

        files := cfg.ManagedFiles
        if len(args) > 0 {
            files = filterFiles(files, args)
        }

        syncedCount := 0
        issuesCount := 0

        for _, f := range files {
            sourcePath, _ := config.ExpandPath(f.SourcePath, cfg)
            if fs.PathExists(sourcePath) {
                syncedCount++
            } else {
                issuesCount++
            }
        }

        if issuesCount == 0 {
            fmt.Printf("✓ %d synced\n", syncedCount)
        } else {
            fmt.Printf("⚠ %d synced, %d issues\n", syncedCount, issuesCount)
        }

        return nil
    }

    // ... rest of function
}
```

**Step 3: Write tests for prompt mode**

```go
func TestStatus_Prompt_MinimalOutput(t *testing.T) {
    t.Run("--prompt returns minimal output", func(t *testing.T) {
        // Arrange
        cfg := CreateTestConfig(t)
        // ... set up managed files

        // Act
        statusCmd.SetArgs([]string{"--prompt"})
        err := runStatus(statusCmd, []string{})

        // Assert
        require.NoError(t, err)
        // Verify output is minimal
    })
}
```

**Step 4: Build, test, and lint**

```bash
go build ./... && go test ./cmd/dotcor/status_test.go -run "Test.*Prompt" && golangci-lint run ./...
```

**Step 5: Commit**

```bash
git add cmd/dotcor/status.go cmd/dotcor/status_test.go
git commit -m "feat: add --prompt flag for shell integration"
```

---

## Task 6.2: Add Backup Diff Command

**Files:**
- Create: `cmd/dotcor/backup-diff.go`

**Step 1: Create backup-diff.go**

```go
package main

import (
    "fmt"
    "os"
    "os/exec"
    "strings"

    "github.com/justincordova/dotcor/internal/config"
    "github.com/justincordova/dotcor/internal/core"
    "github.com/spf13/cobra"
)

var backupDiffCmd = &cobra.Command{
    Use:   "backup-diff [file]",
    Short: "Show changes since last backup",
    Long: `Compare current files to latest backup.

Shows differences between current files and the most recent backup.
Useful for reviewing what would be restored from backup.

Examples:
  dotcor backup-diff ~/.zshrc           # Show diff for specific file
  dotcor backup-diff                       # Show diffs for all files`,
    RunE: runBackupDiff,
}

func init() {
    backupDiffCmd.Flags().BoolP("stat", "s", false, "Show summary of changes")
    rootCmd.AddCommand(backupDiffCmd)
}

func runBackupDiff(cmd *cobra.Command, args []string) error {
    showStat, _ := cmd.Flags().GetBool("stat")

    cfg, err := config.LoadConfig()
    if err != nil {
        return fmt.Errorf("loading config: %w", err)
    }

    files := cfg.ManagedFiles
    if len(args) > 0 {
        var filtered []config.ManagedFile
        for _, arg := range args {
            if mf, err := cfg.GetManagedFile(arg); err == nil {
                filtered = append(filtered, *mf)
            }
        }
        files = filtered
    }

    if len(files) == 0 {
        return fmt.Errorf("no files to compare")
    }

    for _, mf := range files {
        sourcePath, err := config.ExpandPath(mf.SourcePath, cfg)
        if err != nil {
            continue
        }

        // Get latest backup
        latestBackup, err := core.GetLatestBackup(mf.SourcePath, cfg)
        if err != nil {
            fmt.Printf("  - %s: no backup\n", mf.SourcePath)
            continue
        }

        // Show diff
        fmt.Printf("Changes since backup for %s:\n", mf.SourcePath)
        fmt.Printf("  Backup: %s (%s)\n", latestBackup.BackupPath, latestBackup.Timestamp.Format("2006-01-02 15:04:05"))

        if showStat {
            showBackupStat(sourcePath, latestBackup.BackupPath)
        } else {
            showBackupDiff(sourcePath, latestBackup.BackupPath)
        }
        fmt.Println("")
    }

    return nil
}

func showBackupDiff(sourcePath, backupPath string) {
    cmd := exec.Command("diff", "-u", backupPath, sourcePath)
    cmd.Stdout = os.Stdout
    cmd.Stderr = os.Stderr
    cmd.Run()
}

func showBackupStat(sourcePath, backupPath string) {
    cmd := exec.Command("diff", "--stat", backupPath, sourcePath)
    output, err := cmd.CombinedOutput()
    if err != nil {
        fmt.Printf("  Error getting diff stat: %v\n", err)
        return
    }
    fmt.Printf("  %s\n", strings.TrimSpace(string(output)))
}
```

**Step 2: Add test file**

Create `cmd/dotcor/backup-diff_test.go`:

```go
func TestBackupDiff_NoBackup_ReturnsError(t *testing.T) {
    t.Run("no backup returns error", func(t *testing.T) {
        // Arrange
        cfg := CreateTestConfig(t)
        // ... set up managed file without backup

        // Act
        err := runBackupDiff(backupDiffCmd, []string{})

        // Assert
        assert.Error(t, err)
    })
}
```

**Step 3: Build and test**

```bash
go build ./... && go test ./cmd/dotcor/backup-diff_test.go && golangci-lint run ./...
```

**Step 4: Commit**

```bash
git add cmd/dotcor/backup-diff.go cmd/dotcor/backup-diff_test.go
git commit -m "feat: add backup-diff command"
```

---

## Task 6.3: Add File Categories Listing

**Files:**
- Modify: `cmd/dotcor/list.go`

**Step 1: Add --categories flag**

```go
func init() {
    listCmd.Flags().BoolP("long", "l", false, "Show detailed information")
    listCmd.Flags().Bool("category", false, "Group files by category")
    listCmd.Flags().Bool("status", false, "Show symlink status")
    listCmd.Flags().Bool("json", false, "Output as JSON")
    listCmd.Flags().Bool("paths-only", false, "Output only paths (for scripting)")
    listCmd.Flags().Bool("categories", false, "Show count by category")
    rootCmd.AddCommand(listCmd)
}
```

**Step 2: Add categories output mode**

```go
func runList(cmd *cobra.Command, args []string) error {
    longFormat, _ := cmd.Flags().GetBool("long")
    byCategory, _ := cmd.Flags().GetBool("category")
    showStatus, _ := cmd.Flags().GetBool("status")
    jsonFormat, _ := cmd.Flags().GetBool("json")
    pathsOnly, _ := cmd.Flags().GetBool("paths-only")
    showCategories, _ := cmd.Flags().GetBool("categories")

    // Load config
    cfg, err := config.LoadConfig()
    if err != nil {
        return fmt.Errorf("loading config: %w\nRun 'dotcor init' first", err)
    }
    configureLogger(cmd, cfg)

    files := cfg.ManagedFiles

    if len(files) == 0 {
        fmt.Println("No files managed by DotCor.")
        fmt.Println("Run 'dotcor add <file>' to start managing dotfiles.")
        return nil
    }

    // Handle categories output
    if showCategories {
        return outputCategories(files)
    }

    // ... rest of function
}

func outputCategories(files []config.ManagedFile) error {
    // Group by category (first directory component of repo path)
    categories := make(map[string][]config.ManagedFile)

    for _, f := range files {
        category := getCategory(f.RepoPath)
        categories[category] = append(categories[category], f)
    }

    // Sort category names
    var categoryNames []string
    for name := range categories {
        categoryNames = append(categoryNames, name)
    }
    sort.Strings(categoryNames)

    // Output by category
    for _, category := range categoryNames {
        categoryFiles := categories[category]
        fmt.Printf("%-20s %3d file(s)\n", category+":", len(categoryFiles))
    }

    fmt.Printf("\n%d file(s) in %d categories\n", len(files), len(categories))
    return nil
}
```

**Step 3: Write tests for categories**

```go
func TestList_Categories_ShowsGroupedFiles(t *testing.T) {
    t.Run("--categories shows grouped files", func(t *testing.T) {
        // Arrange
        cfg := CreateTestConfig(t)
        // ... set up managed files in multiple categories

        // Act
        listCmd.SetArgs([]string{"--categories"})
        err := runList(listCmd, []string{})

        // Assert
        require.NoError(t, err)
        // Verify output contains categories
    })
}
```

**Step 4: Build, test, and lint**

```bash
go build ./... && go test ./cmd/dotcor/list_test.go -run "TestList.*Categories" && golangci-lint run ./...
```

**Step 5: Commit**

```bash
git add cmd/dotcor/list.go cmd/dotcor/list_test.go
git commit -m "feat: add --categories flag to list command"
```

---

## Task 6.4: v0.6.8 Final Verification

**Step 1: Build, test, and lint**

```bash
go build ./... && go test ./... && golangci-lint run ./...
```

**Step 2: Update version**

Modify `cmd/dotcor/main.go:15`:
```go
version = "0.6.8" // Simple features part 1
```

**Step 3: Commit and tag**

```bash
git add cmd/dotcor/main.go
git commit -m "chore: bump version to v0.6.8"
git tag -a v0.6.8 -m "Release v0.6.8: Simple features part 1"
```

---

# Part 7: v0.6.9 - Simple Features Part 2

**Theme:** More useful tools
**Effort:** ~3-5 hours

## Architecture

Add two more features:
1. Find command (search by content or name)
2. Export/import configuration

---

## Task 7.1: Add Find Command

**Files:**
- Create: `cmd/dotcor/find.go`

**Step 1: Create find.go**

```go
package main

import (
    "bufio"
    "fmt"
    "os"
    "os/exec"
    "regexp"
    "strings"

    "github.com/justincordova/dotcor/internal/config"
    "github.com/spf13/cobra"
)

var findCmd = &cobra.Command{
    Use:   "find [pattern]",
    Short: "Search managed files",
    Long: `Search managed files by content or name.

Examples:
  dotcor find "alias ls"         # Search file contents
  dotcor find --name "*.lua"      # Search by filename pattern
  dotcor find --name "*.lua" vim  # Search vim config files only`,
    RunE: runFind,
}

func init() {
    findCmd.Flags().Bool("content", false, "Search file contents (default)")
    findCmd.Flags().Bool("name", false, "Search by filename pattern")
    findCmd.Flags().String("path", "", "Search in specific directory (repo path)")
    rootCmd.AddCommand(findCmd)
}

func runFind(cmd *cobra.Command, args []string) error {
    searchContent, _ := cmd.Flags().GetBool("content")
    searchName, _ := cmd.Flags().GetBool("name")
    searchPath, _ := cmd.Flags().GetString("path")

    if len(args) == 0 {
        return fmt.Errorf("specify search pattern")
    }

    pattern := args[0]

    cfg, err := config.LoadConfig()
    if err != nil {
        return fmt.Errorf("loading config: %w", err)
    }

    files := cfg.ManagedFiles
    if searchPath != "" {
        var filtered []config.ManagedFile
        for _, f := range files {
            if strings.HasPrefix(f.RepoPath, searchPath) {
                filtered = append(filtered, f)
            }
        }
        files = filtered
    }

    if len(files) == 0 {
        fmt.Println("No files to search")
        return nil
    }

    var matches []string

    if searchName {
        // Search by filename pattern
        re, err := regexp.Compile(pattern)
        if err != nil {
            return fmt.Errorf("invalid pattern: %w", err)
        }

        for _, f := range files {
            if re.MatchString(f.SourcePath) {
                matches = append(matches, f.SourcePath)
            }
        }
    } else {
        // Search file contents
        for _, f := range files {
            sourcePath, err := config.ExpandPath(f.SourcePath, cfg)
            if err != nil {
                continue
            }

            content, err := os.ReadFile(sourcePath)
            if err != nil {
                continue
            }

            if strings.Contains(string(content), pattern) {
                matches = append(matches, f.SourcePath)
            }
        }
    }

    if len(matches) == 0 {
        fmt.Printf("No matches found for: %s\n", pattern)
        return nil
    }

    fmt.Printf("Found %d match(es):\n", len(matches))
    for _, match := range matches {
        fmt.Printf("  %s\n", match)
    }

    return nil
}
```

**Step 2: Add tests for find**

Create `cmd/dotcor/find_test.go`:

```go
func TestFind_Content_MatchesFiles(t *testing.T) {
    t.Run("content search finds matching files", func(t *testing.T) {
        // Arrange
        cfg := CreateTestConfig(t)
        // ... create test files with content

        // Act
        findCmd.SetArgs([]string{"test"})
        err := runFind(findCmd, []string{"test"})

        // Assert
        require.NoError(t, err)
        // Verify matches found
    })
}
```

**Step 3: Build and test**

```bash
go build ./... && go test ./cmd/dotcor/find_test.go && golangci-lint run ./...
```

**Step 4: Commit**

```bash
git add cmd/dotcor/find.go cmd/dotcor/find_test.go
git commit -m "feat: add find command"
```

---

## Task 7.2: Add Export/Import Configuration

**Files:**
- Create: `cmd/dotcor/export-config.go`
- Create: `cmd/dotcor/import-config.go`

**Step 1: Create export-config.go**

```go
package main

import (
    "fmt"
    "os"

    "github.com/justincordova/dotcor/internal/config"
    "github.com/spf13/cobra"
)

var exportConfigCmd = &cobra.Command{
    Use:   "export-config",
    Short: "Export configuration to file",
    Long: `Export DotCor configuration to a YAML file.

Useful for:
- Sharing settings across machines
- Version controlling your configuration
- Backing up your configuration

Examples:
  dotcor export-config                      # Export to stdout
  dotcor export-config --output=my-config.yaml  # Export to file`,
    RunE: runExportConfig,
}

func init() {
    exportConfigCmd.Flags().String("output", "", "Output file (default: stdout)")
    rootCmd.AddCommand(exportConfigCmd)
}

func runExportConfig(cmd *cobra.Command, args []string) error {
    outputPath, _ := cmd.Flags().GetString("output")

    cfg, err := config.LoadConfig()
    if err != nil {
        return fmt.Errorf("loading config: %w", err)
    }

    data, err := config.ExportConfig(cfg)
    if err != nil {
        return fmt.Errorf("exporting config: %w", err)
    }

    if outputPath == "" {
        // Write to stdout
        fmt.Println(string(data))
    } else {
        // Write to file
        if err := os.WriteFile(outputPath, data, 0644); err != nil {
            return fmt.Errorf("writing config: %w", err)
        }
        fmt.Printf("Configuration exported to: %s\n", outputPath)
    }

    return nil
}
```

**Step 2: Create import-config.go**

```go
package main

import (
    "fmt"
    "os"

    "github.com/justincordova/dotcor/internal/config"
    "github.com/spf13/cobra"
)

var importConfigCmd = &cobra.Command{
    Use:   "import-config <file>",
    Short: "Import configuration from file",
    Long: `Import DotCor configuration from a YAML file.

Useful for:
- Restoring configuration from backup
- Sharing settings across machines
- Migrating from another instance

Examples:
  dotcor import-config my-config.yaml
  dotcor import-config ~/Downloads/dotcor-config.yaml`,
    RunE: runImportConfig,
}

func init() {
    rootCmd.AddCommand(importConfigCmd)
}

func runImportConfig(cmd *cobra.Command, args []string) error {
    if len(args) == 0 {
        return fmt.Errorf("specify config file to import")
    }

    inputPath := args[0]

    // Read config file
    data, err := os.ReadFile(inputPath)
    if err != nil {
        return fmt.Errorf("reading config file: %w", err)
    }

    // Load config
    cfg, err := config.LoadConfigFromData(data)
    if err != nil {
        return fmt.Errorf("parsing config: %w", err)
    }

    // Validate config
    if err := config.ValidateConfig(cfg); err != nil {
        return fmt.Errorf("invalid config: %w", err)
    }

    // Save to default location
    if err := cfg.SaveConfig(); err != nil {
        return fmt.Errorf("saving config: %w", err)
    }

    fmt.Println("[OK] Configuration imported successfully")
    fmt.Printf("  Repo path: %s\n", cfg.RepoPath)
    fmt.Printf("  Managed files: %d\n", len(cfg.ManagedFiles))

    return nil
}
```

**Step 3: Add LoadConfigFromData function to config**

Modify `internal/config/config.go`:

```go
// LoadConfigFromData loads config from YAML bytes
func LoadConfigFromData(data []byte) (*Config, error) {
    var cfg Config
    if err := yaml.Unmarshal(data, &cfg); err != nil {
        return nil, fmt.Errorf("parsing config: %w", err)
    }

    // Apply migrations
    migrated, err := MigrateConfig(&cfg)
    if err != nil {
        return nil, err
    }

    return migrated, nil
}
```

**Step 4: Add tests for export/import**

Create `cmd/dotcor/export-config_test.go` and `cmd/dotcor/import-config_test.go`.

**Step 5: Build and test**

```bash
go build ./... && go test ./cmd/dotcor/*_test.go -run "Test.*Config" && golangci-lint run ./...
```

**Step 6: Commit**

```bash
git add cmd/dotcor/export-config.go cmd/dotcor/import-config.go internal/config/config.go cmd/dotcor/export-config_test.go cmd/dotcor/import-config_test.go
git commit -m "feat: add export/import configuration commands"
```

---

## Task 7.3: v0.6.9 Final Verification

**Step 1: Build, test, and lint**

```bash
go build ./... && go test ./... && golangci-lint run ./...
```

**Step 2: Update version**

Modify `cmd/dotcor/main.go:15`:
```go
version = "0.6.9" // Simple features part 2
```

**Step 3: Commit and tag**

```bash
git add cmd/dotcor/main.go
git commit -m "chore: bump version to v0.6.9"
git tag -a v0.6.9 -m "Release v0.6.9: Simple features part 2"
```

---

# Part 8: v0.7.0 - Final Polish & Documentation

**Theme:** Final preparation for v1.0.0
**Effort:** ~6-8 hours

## Architecture

Complete final polish items:
1. Add pre-flight display to remaining commands
2. Comprehensive code review
3. Update all documentation
4. Prepare v1.0.0 release notes

---

## Task 8.1: Add Pre-flight Display to Remaining Commands

**Files:**
- Modify: `cmd/dotcor/cleanup.go`
- Modify: `cmd/dotcor/adopt.go`
- Modify: `cmd/dotcor/clone.go`

**Step 1: Add pre-flight to cleanup.go**

Before destructive cleanup, add validation.

**Step 2: Add pre-flight to adopt.go**

Before adopting, add validation.

**Step 3: Add pre-flight to clone.go**

Before cloning, add validation.

**Step 4: Build and test**

```bash
go build ./... && go test ./cmd/dotcor/...
```

**Step 5: Commit**

```bash
git add cmd/dotcor/cleanup.go cmd/dotcor/adopt.go cmd/dotcor/clone.go
git commit -m "feat: add pre-flight validation to remaining commands"
```

---

## Task 8.2: Final Code Review and Linting

**Files:** All Go files

**Step 1: Run comprehensive linting**

```bash
go vet ./... && golangci-lint run ./... && gofmt -s -w ./
```

**Step 2: Fix any lint errors**

Fix all issues found by linting tools.

**Step 3: Commit lint fixes**

```bash
git add .
git commit -m "style: fix linting issues"
```

---

## Task 8.3: Update Documentation

**Files:**
- Modify: `README.md`
- Modify: `docs/TESTING.md`
- Modify: `docs/LOGGING.md`
- Create: `docs/CHANGELOG.md`

**Step 1: Update README.md**

Add new features and flags:
- `--prompt` flag for status
- `--categories` flag for list
- New commands: `backup-diff`, `find`, `export-config`, `import-config`
- Pre-flight validation improvements

**Step 2: Update TESTING.md**

Document new testing patterns for added features.

**Step 3: Update LOGGING.md**

Document color coding conventions and structured logging fields used in new features.

**Step 4: Create CHANGELOG.md**

```markdown
# Changelog

## [v0.6.3] - Critical Code Fixes
- Fixed syntax error in sync.go
- Removed non-standard separator comments
- Fixed typos in comments and error messages
- Fixed nil slice initialization

## [v0.6.4] - UX Progress & Prompts
- Added git progress display for clone and sync
- Added file summaries to confirmation prompts
- Improved user feedback before operations

## [v0.6.5] - UX Colors & Tables
- Added color coding to all command output
- Improved table formatting with dynamic column widths
- Added colored status indicators

## [v0.6.6] - Pre-flight Validation
- Added pre-flight validation system
- Display validation results before operations
- Check symlinks, repo files, backups, git status

## [v0.6.7] - Lock Handling
- Added interactive lock conflict resolution
- Show process age and details
- Options: wait, force, cancel

## [v0.6.8] - Simple Features Part 1
- Added `--prompt` flag to status command
- Added `backup-diff` command
- Added `--categories` flag to list command

## [v0.6.9] - Simple Features Part 2
- Added `find` command with content/name search
- Added `export-config` command
- Added `import-config` command

## [v0.7.0] - Final Polish
- Added pre-flight validation to all commands
- Comprehensive code review and linting
- Updated all documentation
- Prepared for v1.0.0 release

## [v1.0.0] - Production Release
- All v0.2-v0.6 features complete
- UX/UI polish complete
- Reliability improvements complete
- Ready for production use
```

**Step 5: Build and test**

```bash
go build ./... && go test ./...
```

**Step 6: Commit**

```bash
git add README.md docs/TESTING.md docs/LOGGING.md docs/CHANGELOG.md
git commit -m "docs: update documentation for v0.7.0 release"
```

---

## Task 8.4: Final Version Bump and Release Notes

**Step 1: Bump to v1.0.0**

Modify `cmd/dotcor/main.go:15`:
```go
version = "1.0.0" // Production release
```

**Step 2: Update README.md header**

Add v1.0.0 release announcement at top.

**Step 3: Commit and tag**

```bash
git add cmd/dotcor/main.go README.md
git commit -m "chore: bump version to v1.0.0"
git tag -a v1.0.0 -m "Release v1.0.0: Production ready"
git push origin main
git push origin v1.0.0
```

**Step 4: Create release notes**

Write comprehensive release notes with:
- All features added since v0.6.2
- Breaking changes (none)
- Upgrade instructions
- Known issues

---

# Summary

## v0.6.3 Tasks
1. Fix syntax error in sync.go:244
2. Remove "=====" separator comments
3. Fix typos in core files (validator, backup, lock)
4. Fix typos in config and ignore files
5. Fix typos in command files (list, doctor, remove, history)

## v0.6.4 Tasks
6. Add git progress display for clone and sync
7. Add file summaries to confirmation prompts (add, remove)
8. Add formatSize helper function

## v0.6.5 Tasks
9. Add color helper functions (success, warning, error, info, dryrun)
10. Apply colors to all command output
11. Improve table formatting (dynamic widths, colored status)
12. Fix nil slice initialization

## v0.6.6 Tasks
13. Create pre-flight validation system (validation.go)
14. Integrate pre-flight checks into add, remove, restore commands

## v0.6.7 Tasks
15. Add process age to lock errors
16. Add interactive lock menu (wait, force, cancel)
17. Add waitForLock function

## v0.6.8 Tasks
18. Add --prompt flag to status command
19. Create backup-diff command
20. Add --categories flag to list command

## v0.6.9 Tasks
21. Create find command with --content and --name flags
22. Create export-config command
23. Create import-config command
24. Add LoadConfigFromData function to config

## v0.7.0 Tasks
25. Add pre-flight display to remaining commands
26. Comprehensive code review and linting
27. Update documentation (README, TESTING, LOGGING)
28. Create CHANGELOG.md
29. Bump version to v1.0.0
30. Create release notes

## Key Files Modified

```
cmd/dotcor/main.go             - Version bumps, color helpers
cmd/dotcor/add.go             - Pre-flight validation
cmd/dotcor/remove.go           - Pre-flight validation, colors
cmd/dotcor/sync.go             - Syntax fix, git progress, pre-flight, nil slice
cmd/dotcor/status.go           - --prompt flag, --categories flag, colors
cmd/dotcor/restore.go          - Pre-flight validation, colors
cmd/dotcor/clone.go            - Git progress, pre-flight
cmd/dotcor/list.go             - --categories flag, tables, colors
cmd/dotcor/doctor.go           - Remove separators, colors
cmd/dotcor/adopt.go            - Pre-flight, colors
cmd/dotcor/cleanup.go          - Pre-flight, colors
cmd/dotcor/history.go           - Typo fixes
cmd/dotcor/find.go              - New command
cmd/dotcor/backup-diff.go       - New command
cmd/dotcor/export-config.go     - New command
cmd/dotcor/import-config.go      - New command
internal/core/transaction.go      - Remove separators
internal/core/validator.go        - Typo fixes
internal/core/backup.go          - Typo fixes
internal/core/lock.go            - Typo fixes, interactive handling
internal/core/validation.go       - New file
internal/config/paths.go        - Typo fixes
internal/core/ignore.go          - Typo fixes
internal/git/git.go             - CloneWithProgress function
internal/config/config.go        - LoadConfigFromData function
cmd/dotcor/*_test.go           - Tests for new features
docs/CHANGELOG.md               - New file
README.md                       - Updated for v1.0.0
docs/TESTING.md                 - Updated for new features
docs/LOGGING.md                 - Updated for colors
```

## Testing Strategy

- **Unit tests**: Each feature has dedicated unit tests in `cmd/dotcor/*_test.go`
- **Integration tests**: Update `tests/integration_test.go` for new workflows
- **Manual verification**: Run all commands to verify behavior

## Total Estimated Effort

| Version | Tasks | Hours |
|---------|--------|-------|
| v0.6.3 | 6 tasks (critical fixes) | 2-3 |
| v0.6.4 | 3 tasks (progress & prompts) | 6-9 |
| v0.6.5 | 4 tasks (colors & tables) | 4-6 |
| v0.6.6 | 2 tasks (pre-flight) | 6-8 |
| v0.6.7 | 2 tasks (lock handling) | 4-5 |
| v0.6.8 | 3 tasks (features) | 4-6 |
| v0.6.9 | 4 tasks (features) | 3-5 |
| v0.7.0 | 5 tasks (polish & docs) | 6-8 |
| **Total** | **29 tasks** | **35-50 hours** |

---

**Plan complete.** Ready for execution using subagent-driven-development or parallel session.
